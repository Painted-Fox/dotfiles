"hg6883

loadplugins '\.(js|penta)$'
group user


" My customizations.
colorscheme zenburn

set followhints=1
set showtabline=multitab
set showstatuslinks=command

" Highlight all search matches, not just the first.
set hlfind

" FASTER PLEASE
nmap -builtin j 3j
nmap -builtin k 3k

" Flash Blocker whitelist
set fbwhitelist=pandora.com,youtube.com,newgrounds.com,maps.google.com

" Some sites already enable vim-like navigation.  Don't intercept these keys.
set passkeys+=mail.google.com:jk<CR>,gi
set passkeys+=www.google.com/reader:jk<CR>,v,gh,gs
